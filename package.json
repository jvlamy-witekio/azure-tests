{
  "name": "azure-tests",
  "version": "1.0.0",
  "description": "",
  "main": "dist/functions/index.js",
  "scripts": {
    "preinstall": "npm run install:api && npm run install:angular && npm run install:functions",
    "install:api": "npm i --prefix src/api",
    "install:angular": "npm i --prefix src/angular",
    "install:functions": "npm i --prefix src/functions",
    "build": "npm run build:api && npm run build:angular && npm run build:functions",
    "build:api": "cd src/api && npm run build",
    "build:angular": "cd src/angular && npm run build",
    "build:functions": "cd src/functions && npm run build",
    "start:api": "cd src/api && npm run start:dev",
    "start:angular": "cd src/angular && npm run start",
    "start:functions": "cd src/functions && npm run start",
    "watch:functions": "cd src/functions && npm run watch",
    "dev": "npm-run-all --parallel start:api start:functions watch:functions",
    "docker-up": "docker compose up --build --remove-orphans",
    "docker-down": "docker compose down"
  },
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.0.0"
  }
}